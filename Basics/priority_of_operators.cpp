#include <iostream>

int main()
{
    double x = 10;
    double y = 5;
    double z = (x + 10) / (3 * y);

    std::cout << z;

    return 0;
}
// prioritzation of methamethical expressions is the same as in normal math.
// ()
// * and /
// + and -