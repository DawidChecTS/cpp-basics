#include <iostream>

using namespace std;

int main()
{
    int file_size;               // declaration of the variable file_size with data type integer
    file_size = 100;             // assignemnt of file_size variable
    double sales = 9.99;         // initialization of sales variable
    
    int a = 1;                   // exercise to swap the value of this two variables
    int b = 2;

    b = a; 
    cout << b;                   // cout means character output, this line is called a statment, it tells the system what to do.
    return 0;                    // we return the zero to tell the system that this program going to terminate correctly
}

