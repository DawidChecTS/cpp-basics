#include <iostream>

int main() {

    // INTEGER
    int number; // declaretion of an integer
    number = 12; // assignment 
    std::cout << number << '\n'; // print out the assinged number integer

    // DOUBLE
    double price = 11.4;// declaration and assignment of a double price
    std::cout << price << '\n'; // print out the assigned price double

    // CHARACTER
    char grade = 'A'; // declaration of A character
    std::cout << grade << '\n'; // print out the assigned grade character

    // STRING
    std::string name = "Dawid"; // declaration and asssignment of STRING word
    std::cout << name << '\n'; // print out string name
    
    // BOOLEAN
    bool power = true; // declaration and assignment of boolean student
    bool light = false; // declaration and assignment of boolean
    std::cout << power << '\n' << light << '\n'; // print out both power and light booleans

    std::cout << number << " is the price, not: " << price << '\n'; // how to print out both text and variables in one line




    return 0;
}

// Variables are string representations of some values
// To declere a variable we need to list the data type of what we are storing exacly 